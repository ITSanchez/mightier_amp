#include "../../Flutter/Flutter-Release.xcconfig"
#include "Warnings.xcconfig"

// Apple Silicon only — avoids Flutter/lipo universal verify failure on macOS 27.
ARCHS = arm64
EXCLUDED_ARCHS = x86_64
ONLY_ACTIVE_ARCH = YES

